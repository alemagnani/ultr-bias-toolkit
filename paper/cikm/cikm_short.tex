\documentclass[sigconf,anonymous]{acmart}
\usepackage{amsmath, amsthm}
\usepackage{graphicx}
\usepackage{url}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{multirow}

\newtheorem{theorem}{Theorem}
\newtheorem{corollary}[theorem]{Corollary}

\setcopyright{acmlicensed}
\copyrightyear{2026}
\acmYear{2026}
\acmConference[CIKM '26]{ACM CIKM}{October 2026}{Toronto, Canada}

\title{Harmonic Weighting is Count-Only Inverse-Variance Weighting:
A Rigorous Asymptotic Analysis of Intervention Harvesting on Heavy-Tailed Click Logs}

\author{Anonymous}
\affiliation{\institution{Anonymous}\country{}}
\renewcommand{\shortauthors}{Anonymous}

\begin{document}

\begin{abstract}
Intervention-harvesting estimators of position bias~\cite{agarwal2019estimating}
combine per-cell click-rate estimates across many $(q,d,\mathrm{position})$
cells with widely varying impression counts. We make three contributions.
\textbf{(i)} We identify \emph{harmonic} weighting
$\omega_i=N_k^i N_{k'}^i/(N_k^i+N_{k'}^i)$ as the count-only inverse-variance
optimum -- the classical generalised-least-squares solution of
Aitken~\cite{aitken1934least} applied to ULTR. We give an unconditional
Cauchy--Schwarz guarantee that harmonic reduces the delta-method variance
surrogate $V_k+V_{k'}$ for \emph{any} impression counts, and characterise the
exact asymmetric oracle of which harmonic is the $\alpha=\beta$ special case.
\textbf{(ii)} We replace bootstrap-based variance evaluation -- which is
provably the wrong instrument on heavy-tailed pair distributions -- with the
plug-in delta-method asymptotic variance $\widetilde V(\omega)$ of the
self-normalised weighted ratio estimator~\cite{owen2013mc,vdv1998asymptotic},
and report \emph{asymptotic relative efficiency} (ARE) as the headline
finite-sample diagnostic. \textbf{(iii)} On the full KDD Cup 2012 Track 2
sponsored-search log (235M impressions; 4.6M interventional pairs), harmonic
achieves $\mathrm{ARE}=4.53$ versus uniform (rising to $\sim 5\times$ on the
high-information quartile), with plug-in $\widetilde V$ agreeing with empirical
click-resample SE to within $1\%$. A Cochran-$Q$ heterogeneity test on
high-information cells rejects PBM at $z\approx 253\sigma$, cleanly separating
estimator efficiency from model misspecification. Synthetic ($43\%$--$54\%$ MSE
reduction at high imbalance) and semi-synthetic Yahoo-LTR ($40\%$ variance
reduction at $r=50$) experiments with ground truth corroborate the efficiency
claim.
\end{abstract}

\maketitle

\section{Introduction}
\label{sec:intro}

Position bias systematically corrupts click signals used for
learning-to-rank~\cite{joachims2017unbiased}; intervention
harvesting~\cite{agarwal2019estimating} estimates position-bias ratios
$R_{k,k'}=p_k/p_{k'}$ from naturally occurring ranking variations without
explicit randomisation. On production click logs, the per-cell counts that
feed this estimator are extraordinarily heavy-tailed. On the full
KDD Cup 2012 Track 2 release (235M impressions; 4.6M interventional pairs
at adjacent positions $(1,2)$), \textbf{60.7\% of pairs have
$N_1+N_2\le 5$ while 0.1\% of pairs carry 42\% of all
impressions}~\cite{kddcup2012}. Public benchmarks hide this shape:
Baidu-ULTR~\cite{zou2022large} has 94\% of $(q,d,\mathrm{pos})$ cells with
$N=1$ after deduplication and 78\% of adjacent-pair ratios within $[0.5,2]$.

Equal-weighting heteroscedastic estimators is statistically suboptimal by a
century of classical theory: Aitken's generalised-least-squares
theorem~\cite{aitken1934least} mandates inverse-variance weights, a result
the meta-analysis literature~\cite{hedges1985meta,borenstein2009meta} has
championed for fifty years. Recent ULTR
work~\cite{agarwal2019estimating,wang2018position} introduced \emph{harmonic}
weighting $\omega_i=N_k^i N_{k'}^i/(N_k^i+N_{k'}^i)$ to reduce variance versus
uniform on heavy-tailed counts, but existing validation has relied on
\emph{bootstrap} resampling -- which we show is provably the wrong instrument
for inverse-variance-style weights on singleton-dominated data
(Section~\ref{sec:plugin}).

\textbf{Contributions.}
\textbf{(1)} We identify harmonic as the count-only inverse-variance optimum
(Theorem~\ref{thm:ivw}), prove an unconditional Cauchy--Schwarz reduction of
the delta-method variance surrogate (Theorem~\ref{thm:cs}), and characterise
the exact asymmetric oracle of which harmonic is the $\alpha=\beta$ special
case (Theorem~\ref{thm:asym}).
\textbf{(2)} We adopt the plug-in delta-method asymptotic variance
$\widetilde V(\omega)$ as the appropriate finite-sample efficiency
diagnostic on real click logs, replacing bootstrap.
\textbf{(3)} On the full KDD log we measure
$\mathrm{ARE}(\text{harmonic},\text{uniform})=4.53$, calibrated within $1\%$,
and reject PBM at $z\approx 253\sigma$ via Cochran's $Q$.
Synthetic and Yahoo-LTR experiments with ground truth corroborate.

\section{Estimator and Theory}
\label{sec:method}

Under PBM, $P(\mathrm{click}\mid q,d,k)=p_k\,r_{q,d}$. Given an interventional
set $I_{k,k'}$ of $(q,d)$ pairs that appeared at both positions $k$ and $k'$,
the position-bias ratio is estimated as~\cite{agarwal2019estimating}
\begin{equation}
\hat R_{k,k'}=\frac{A}{B}=\frac{\sum_i \omega_i\, \hat\theta_k^i}
                              {\sum_i \omega_i\, \hat\theta_{k'}^i},
\quad \hat\theta_k^i=\tfrac{C_k^i}{N_k^i}.
\label{eq:Rhat}
\end{equation}
This is a self-normalised weighted ratio estimator. Any count-only weight
$\omega_i=\omega(N_k^i,N_{k'}^i)$ preserves ratio-unbiasedness under
PBM~\cite{agarwal2019estimating}.

\paragraph{Conditional variance.}
By the delta method~\cite{vdv1998asymptotic},
$\mathrm{Var}(\hat R\mid\mathbf N)\approx R^2[\alpha V_k(\omega)+\beta
V_{k'}(\omega)]$ with $V_r(\omega)=\sum_i\omega_i^2/N_r^i/(\sum_i\omega_i)^2$,
$\alpha=(1-p_k)/p_k$, $\beta=(1-p_{k'})/p_{k'}$.

\paragraph{Harmonic = count-only IVW.}
For Bernoulli observations, $\mathrm{Var}(\hat\theta_k^i)=\theta_k^i(1-\theta_k^i)/N_k^i$.
With $\theta$ treated as constant across cells, the IVW optimum of
Aitken~\cite{aitken1934least} for the pair $(k,k')$ is the harmonic mean.

\begin{theorem}[Count-only IVW characterisation]
\label{thm:ivw}
Among all non-negative weights $\omega_i=\omega(N_k^i,N_{k'}^i)$, harmonic
weighting minimises $V_k(\omega)+V_{k'}(\omega)$.
\end{theorem}

\begin{theorem}[Unconditional surrogate guarantee]
\label{thm:cs}
For any impression counts $\{N_k^i,N_{k'}^i\}_{i=1}^n$,
$\;V_k(\omega^h)+V_{k'}(\omega^h)\le V_k(\mathbf 1)+V_{k'}(\mathbf 1)$.
\end{theorem}

\begin{proof}[Sketch]
Substituting $\omega_i^h=N_k^i N_{k'}^i/(N_k^i+N_{k'}^i)$ collapses the LHS to
$1/W$ with $W=\sum_i\omega_i^h$. The RHS is
$(1/n^2)\sum_i 1/\omega_i^h$. Cauchy--Schwarz gives
$n^2\le W\sum_i 1/\omega_i^h$.
\end{proof}

\begin{theorem}[Exact asymmetric oracle]
\label{thm:asym}
$\alpha V_k(\omega)+\beta V_{k'}(\omega)\ge(\sum_i 1/s_i)^{-1}$ with
$s_i=\alpha/N_k^i+\beta/N_{k'}^i$, attained at
$\omega_i^\star\propto N_k^i N_{k'}^i/(\alpha N_{k'}^i+\beta N_k^i)$.
Harmonic is the $\alpha=\beta$ special case.
\end{theorem}

Theorem~\ref{thm:cs} is unconditional in the counts -- a property that
distinguishes harmonic from the min-count heuristic of~\cite{agarwal2019estimating},
which has no such guarantee. Theorem~\ref{thm:asym} provides the oracle
benchmark; in synthetic experiments substituting the true $\alpha,\beta$ into
$\omega^\star$ yields variance within $\pm 2\%$ of harmonic.

\section{Plug-in Asymptotic Variance: the Right Instrument}
\label{sec:plugin}

\paragraph{$\widetilde V$ in closed form.}
With $A=\sum_i\omega_i\theta_k^i$, $B=\sum_i\omega_i\theta_{k'}^i$, and
clicks at $k$ and $k'$ conditionally independent given counts, the delta
method gives
\begin{equation}
\widetilde V(\omega)=\frac{\sum_i\omega_i^2\,\theta_k^i(1-\theta_k^i)/N_k^i}{A^2}
+\frac{\sum_i\omega_i^2\,\theta_{k'}^i(1-\theta_{k'}^i)/N_{k'}^i}{B^2},
\label{eq:Vtilde}
\end{equation}
$\mathrm{SE}(\hat R)=|\hat R|\sqrt{\widetilde V(\omega)}$. Plugging in
$\hat\theta_i$ (with light shrinkage toward the marginal) yields the textbook
self-normalised IS variance estimator~\cite{owen2013mc,kong1992ess,swaminathan2015snips}.

\paragraph{ARE.} For schemes $a,b$,
$\mathrm{ARE}(a,b)=\widetilde V(\omega_b)/\widetilde V(\omega_a)$:
scheme $b$ needs $\mathrm{ARE}(a,b)$ times the data of $a$ for equal
precision. $\widetilde V$ scales as $1/\mathrm{ESS}(\omega)$, with
Kong's effective sample size~\cite{kong1992ess}
$\mathrm{ESS}(\omega)=(\sum_i\omega_i)^2/\sum_i\omega_i^2$.

\paragraph{Why bootstrap fails on heavy-tailed click logs.}
\emph{(i)} Uniform averages over $\sim 60\%$ near-singleton pairs, producing a
smooth bootstrap distribution whose tightness reflects averaging-of-noise, not
estimator precision. \emph{(ii)} Pair resampling perturbs the high-information
cells that drive any inverse-variance estimator, penalising concentrated weight
schemes by construction. \emph{(iii)} Bootstrap conflates estimator efficiency
with PBM misspecification; $\widetilde V$ does not (Section~\ref{sec:cochran}).

\paragraph{Calibration.}
On KDD at $f=0.001$, plug-in SE matches empirical click-resample SE within
$1\%$ for both uniform ($0.1306$ vs.\ $0.1321$) and harmonic ($0.1113$
vs.\ $0.1120$). The plug-in is correct on this data.

\section{Real-Log Results: KDD Cup 2012 Track 2}
\label{sec:kdd}

\paragraph{Data.} Full release: 149.6M raw rows expanding to 235.6M
impressions, 63.8M unique $(q,\mathrm{ad},\mathrm{pos})$ cells, 8.2M clicks
(CTR $3.49\%$). Pair $(1,2)$ interventional set: 4.61M $(q,\mathrm{ad})$
pairs, 132.3M impressions, median pair size 4, max 2.47M; 60.7\% of pairs
have $N_1+N_2\le 5$.

\begin{table}[t]
\caption{Plug-in delta-method asymptotic variance $\widetilde V$, SE of
$\hat R$, ARE vs.\ uniform, and ESS on full KDD Cup 2012 Track 2
($n_{\mathrm{pairs}}=4.61$M). Harmonic, min, and the count-only oracle
all reach $\mathrm{ARE}\approx 4.5$. The full-$\theta$ oracle is
pathological because per-cell $\hat\theta_i$ is too noisy at this scale.}
\label{tab:are_full}
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Scheme                     & $\hat R$ & $\mathrm{SE}$ & $\mathrm{ARE}$ & ESS \\
\midrule
Uniform                    & 1.683 & 4.24e-3 & 1.00          & 4.61M \\
Min~\cite{agarwal2019estimating}            & 1.712 & 2.05e-3 & 4.41          & 2{,}526 \\
\textbf{Harmonic}          & 1.710 & 2.02e-3 & \textbf{4.53} & 1{,}849 \\
Count-oracle (0.5,1)       & 1.715 & 2.02e-3 & 4.57          & 1{,}760 \\
$\theta$-aware oracle      & 2.343 & 0.148   & 0.002         & 1.1 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\caption{Stratified ARE(harmonic, uniform) by $N_1+N_2$ quartile on full KDD.
Schemes are tied on the noise-dominated quartiles; harmonic dominates
$\sim 5\times$ on the high-information quartile where the actual estimation
signal lives.}
\label{tab:strat}
\centering
\small
\begin{tabular}{lrrr}
\toprule
$N_1+N_2$ quartile          & $n$ pairs & $\widetilde V_U$  & $\mathrm{ARE}(h,u)$ \\
\midrule
Lowest                  & 1.99M & 2.3e-5 & 1.02 \\
Middle-low              & 1.38M & 1.9e-5 & 1.09 \\
\textbf{Highest}        & 1.25M & 7.6e-6 & \textbf{4.98} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Headline (Table~\ref{tab:are_full}).}
$\mathrm{ARE}(\text{harmonic},\text{uniform})=4.53$.
\textbf{Uniform requires $\sim 4.5\times$ the impressions of harmonic for
matching precision}, with $\mathrm{SE}_U/\mathrm{SE}_H\approx 2.13$. The
min heuristic~\cite{agarwal2019estimating} achieves $4.41$, the count-only
oracle $4.57$; harmonic is the principled parameter-free optimum.

\paragraph{Stratified ARE (Table~\ref{tab:strat}).}
Schemes tie on noise singletons (where there is nothing to estimate) and
harmonic dominates uniform by $5\times$ on the high-information quartile.
The gain comes from \emph{not} treating singletons as equal to
million-impression cells.

\paragraph{Cochran's $Q$ rejects PBM at $z\approx 253\sigma$.}
\label{sec:cochran}
On the 16,132 high-information pairs ($\ge 5$ clicks at each of positions
$1$ and $2$), $Q=6.17{\times}10^4$, $\mathrm{df}=16{,}131$,
$Q/\mathrm{df}=3.83$, $z\approx 253\sigma$.
This holds across scales ($z\approx 50$ at $f=0.1$, $z\approx 7.6$ at
$f=0.01$). PBM rejection explains why schemes converge to slightly different
$\hat R$ on KDD (uniform 1.68, harmonic 1.71, $\theta$-oracle 2.34): Theorem~1
ratio-unbiasedness~\cite{agarwal2019estimating} requires PBM, which is
violated -- consistent with prior findings on real
logs~\cite{yue2010beyond,wang2018position}. The ARE comparison is unaffected
as it is a model-internal efficiency statement.

\section{Synthetic and Yahoo-LTR Validation}
\label{sec:synth}

To validate the ARE prediction in settings with ground truth, we run two
controlled experiments.

\paragraph{Anchor-item imbalance sweep (synthetic).}
Following~\cite{agarwal2019estimating}, $M{=}10$ positions, $p_k=k^{-1}$,
anchor-item model with traffic split $s$ and imbalance ratio $r$, 500 trials.
At $s=0.80$, $r=100$ harmonic reduces variance by $43.6\%$ vs.\ uniform,
$5\%$ vs.\ min; reduction grows to $\sim 54\%$ at $s=0.90,r=50$.
Across the $(s,r)$ grid, the data-equivalence factor
$\mathrm{DEF}=b_{\mathrm{uni}}/b_{\mathrm{harm}}$ has median $1.31$ and
maximum $1.94$.

\paragraph{Yahoo-LTR semi-synthetic.}
Same simulator but with real graded relevance from Yahoo Learning to Rank
Challenge Set~1 (6936 test queries; clicks simulated via PBM). Harmonic
reduces variance by $\sim 40\%$ vs.\ uniform at imbalance $r=50$
(Var $0.00015 \to 0.00009$; ${\sim}458\times$ lower MSE than aggressive
clipping). Production-style traffic simulation with $K{=}5$ concurrent
rankers (one default at $80\%$ + four treatment at $5\%$ each, swap
probability $0.5$): under uniform traffic only $0.4\%$ of pairs exceed
ratio~$10$, but under production traffic $16.7\%$ do; harmonic's MSE
reduction grows from $6.3\%$ to $\textbf{17.2\%}$, confirming the gain
scales with traffic-induced imbalance.

\section{Conclusion}

Framing harmonic weighting as the count-only inverse-variance optimum
(Theorems~\ref{thm:ivw}--\ref{thm:asym}) and replacing bootstrap with the
plug-in delta-method variance $\widetilde V$ (Eq.~\ref{eq:Vtilde}) yields
rigorous, calibration-verified efficiency comparisons on heavy-tailed real
click logs. On the full KDD Cup 2012 Track 2 log,
$\mathrm{ARE}(\text{harmonic},\text{uniform})=4.53$, rising to $\sim 5\times$
on information-bearing cells, with PBM heterogeneity quantified separately by
Cochran-$Q$ at $z\approx 253\sigma$. Future work: shrinkage variants of the
$\theta$-aware oracle (Theorem~\ref{thm:asym} with $\hat\theta_i$
regularised); click models beyond
PBM~\cite{wang2018position,yue2010beyond,chuklin2015clickmodels} where the
intervention-harvesting family must be reformulated.

\section*{GenAI Usage Disclosure}
The authors used a large-language-model assistant for code generation
(KDD aggregation, plug-in variance script, LaTeX table formatting), and for
copy-editing. All experimental design, mathematical derivations
(Theorems~\ref{thm:ivw}--\ref{thm:asym} and Eq.~\eqref{eq:Vtilde}),
interpretation, citations, and final wording were authored and verified by
the authors. No LLM-generated content was used without human review. Full
code, scripts, and data-processing pipelines are provided as supplementary
material.

\bibliographystyle{ACM-Reference-Format}
\begin{thebibliography}{15}

\bibitem{agarwal2019estimating}
A.~Agarwal, X.~Wang, C.~Li, M.~Bendersky, and M.~Najork.
\newblock Estimating position bias without intrusive interventions.
\newblock In {\em WSDM}, 2019.

\bibitem{aitken1934least}
A.~C. Aitken.
\newblock On least squares and linear combination of observations.
\newblock {\em Proc.\ Royal Soc.\ Edinburgh}, 55:42--48, 1934.

\bibitem{borenstein2009meta}
M.~Borenstein, L.~V. Hedges, J.~P.~T. Higgins, and H.~R. Rothstein.
\newblock {\em Introduction to Meta-Analysis}.
\newblock Wiley, 2009.

\bibitem{chuklin2015clickmodels}
A.~Chuklin, I.~Markov, and M.~de~Rijke.
\newblock {\em Click Models for Web Search}.
\newblock Morgan \& Claypool, 2015.

\bibitem{hedges1985meta}
L.~V. Hedges and I.~Olkin.
\newblock {\em Statistical Methods for Meta-Analysis}.
\newblock Academic Press, 1985.

\bibitem{joachims2017unbiased}
T.~Joachims, A.~Swaminathan, and T.~Schnabel.
\newblock Unbiased learning-to-rank with biased feedback.
\newblock In {\em WSDM}, 2017.

\bibitem{kddcup2012}
Tencent.
\newblock {KDD Cup 2012 Track 2}: predict the click-through rate of ads.
\newblock Competition release, 2012.

\bibitem{kong1992ess}
A.~Kong.
\newblock A note on importance sampling using standardized weights.
\newblock Tech.\ Rep.\ 348, U.\ Chicago Statistics, 1992.

\bibitem{owen2013mc}
A.~B. Owen.
\newblock {\em Monte Carlo Theory, Methods and Examples}, ch.\ 9.
\newblock 2013.

\bibitem{swaminathan2015snips}
A.~Swaminathan and T.~Joachims.
\newblock The self-normalized estimator for counterfactual learning.
\newblock In {\em NeurIPS}, 2015.

\bibitem{vdv1998asymptotic}
A.~W. van~der Vaart.
\newblock {\em Asymptotic Statistics}, ch.\ 3.
\newblock Cambridge, 1998.

\bibitem{wang2018position}
X.~Wang, M.~Bendersky, D.~Metzler, and M.~Najork.
\newblock Position bias estimation for unbiased learning to rank in personal search.
\newblock In {\em WSDM}, 2018.

\bibitem{yue2010beyond}
Y.~Yue, R.~Patel, and H.~Roehrig.
\newblock Beyond position bias: examining result attractiveness as a source of presentation bias.
\newblock In {\em WWW}, 2010.

\bibitem{zou2022large}
L.~Zou et~al.
\newblock A large-scale search dataset for unbiased learning to rank.
\newblock In {\em NeurIPS}, 2022.

\end{thebibliography}

\end{document}
